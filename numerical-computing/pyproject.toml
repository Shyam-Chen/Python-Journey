[project]
name = "numerical-computing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.4",
]

[dependency-groups]
dev = [
    "marimo>=0.23.0",
    "ruff>=0.15.9",
]

[tool.ruff]
line-length = 100
