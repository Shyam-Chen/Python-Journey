[project]
name = "data-processing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "polars>=1.40.1",
]

[dependency-groups]
dev = [
    "marimo>=0.23.4",
    "ruff>=0.15.12",
]

[tool.ruff]
line-length = 100
